{
  "data": {
    "name": "CF-Gateway-DNSTunnel \u2014 DNS Tunneling / C2 Beaconing (Gateway DNS)",
    "description": "Behavioural detection of DNS tunneling, DGA/C2 beaconing, and data-in-DNS exfiltration on Cloudflare Gateway DNS (class_uid 4003). Groups by source + registered-zone and fires on: (a) >=10 high-entropy AND >=10 unique labels (DGA/beaconing), (b) >=5 long AND >=5 unique labels (dnscat2/iodine tunneling), or (c) >=3 long TXT queries (base32 data-in-DNS exfil). Volume alone is NOT a trigger \u2014 legit cloud services (Azure/AWS/CDN) resolve many unique structured subdomains (validated FPs tuned out: cloudapp.azure.com high-cardinality, saas.atlassian.com long label). Uses unmapped.QueryName (OCSF query.hostname empty here). MITRE T1071.004 / T1048 / T1568.002. First output column is detection_time. Covers OneFlare scenario 05.",
    "queryType": "scheduled",
    "queryLang": "2.0",
    "severity": "High",
    "status": "Active",
    "expirationMode": "Permanent",
    "treatAsThreat": "UNDEFINED",
    "networkQuarantine": false,
    "s1ql": "",
    "scheduledParams": {
      "query": "class_uid=4003 dataSource.cloudflare_dataset='Gateway DNS' unmapped.QueryName=*\n| let fqdn        = lower(unmapped.QueryName)\n| let label       = replace(fqdn, '\\\\..*', '')\n| let zone        = replace(fqdn, '^.*\\\\.([^.]+\\\\.[^.]+\\\\.[^.]+)$', '$1')\n| let label_len   = len(label)\n| let digit_cnt   = len(replace(label, '[^0-9]', ''))\n| let digit_ratio = label_len > 0 ? digit_cnt / label_len : 0\n| let is_txt      = query.type in:anycase ('txt')\n| let src_ip      = src_endpoint.ip\n| let host        = device.name\n| group\n    total_queries = count(),\n    uniq_labels   = estimate_distinct(label),\n    max_label_len = max(label_len),\n    long_labels   = count(label_len >= 40),\n    txt_long      = count(is_txt AND label_len >= 20),\n    hi_entropy    = count(label_len >= 12 AND digit_ratio >= 0.15),\n    device_uid    = any(device.uid),\n    evidence      = max_by(unmapped.QueryName, label_len),\n    first_seen    = oldest(timestamp),\n    last_seen     = newest(timestamp)\n  by src_ip, host, zone\n| filter (hi_entropy >= 10 AND uniq_labels >= 10) OR (long_labels >= 5 AND uniq_labels >= 5) OR txt_long >= 3\n| let reason = txt_long >= 3 ? 'Data-in-DNS exfil (TXT long labels)' : ((long_labels >= 5 AND uniq_labels >= 5) ? 'Long-label DNS tunneling' : 'DGA / high-entropy beaconing')\n| let detection_time = simpledateformat(last_seen, 'yyyy-MM-dd HH:mm:ss z')\n| sort -uniq_labels\n| columns detection_time, src_ip, host, zone, reason, total_queries, uniq_labels, long_labels, txt_long, hi_entropy, max_label_len, evidence, device_uid, first_seen\n| limit 100",
      "runIntervalMinutes": 1,
      "lookbackWindowMinutes": 15,
      "threshold": {
        "value": 0,
        "operator": "Greater"
      }
    },
    "_entityMappings_pending_feature": [
      {
        "columnName": "src_ip"
      },
      {
        "columnName": "host"
      },
      {
        "columnName": "device_uid"
      }
    ]
  },
  "filter": {
    "siteIds": [
      "2433185103040607397"
    ]
  }
}