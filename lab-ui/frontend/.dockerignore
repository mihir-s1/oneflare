# Never copy host-built artifacts into the image — they carry the host platform's
# native binaries (e.g. macOS rollup) and break the Linux build. Let `npm install`
# resolve the correct platform binaries inside the image.
node_modules
dist
npm-debug.log*
.vite
.DS_Store
